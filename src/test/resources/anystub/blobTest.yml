request0:
  exception: []
  keys: DROP TABLE BLOBREPORT IF EXISTS
  values: 'false'
request1:
  exception: []
  keys: |-
    CREATE TABLE BLOBREPORT(
    ID BIGINT PRIMARY KEY AUTO_INCREMENT,
    NAME VARCHAR(255) NOT NULL,
    IMAGE BLOB
    );
  values: 'false'
request2:
  exception: []
  keys: ['insert into BLOBREPORT (NAME, IMAGE) values (?, ?)', '1', TEXT, setBlob,
    '2']
  values: '1'
request3:
  exception: []
  keys: ['insert into BLOBREPORT (NAME, IMAGE) values (?, ?)', getGeneratedKeys]
  values: ['1', ID, BIGINT, '-5', '19', '0', '1']
request4:
  exception: []
  keys: ['select * from BLOBREPORT where id =?', setObject, '1']
  values: ['3', ID, BIGINT, '-5', '19', '0', NAME, VARCHAR, '12', '255', '0', IMAGE,
    BLOB, '2004', '2147483647', '0']
request5:
  exception: []
  keys: [resultSet, 'select * from BLOBREPORT where id =?', next]
  values: 'true'
request6:
  exception: []
  keys: [resultSet, 'select * from BLOBREPORT where id =?', getBlob, IMAGE]
  values: blob content
request7:
  exception: []
  keys: [resultSet, 'select * from BLOBREPORT where id =?', next, '#1']
  values: 'false'
