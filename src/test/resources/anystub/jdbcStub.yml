request0:
  exception: []
  keys: DROP ALIAS SP_HELLO if Exists
  values: 'false'
request1:
  exception: []
  keys: |-
    CREATE ALIAS SP_HELLO AS $$
    String spHello(String value) {
        return "HELLO: "+value;
    }
    $$;
  values: 'false'
request2:
  exception: []
  keys: call SP_HELLO('XX');
  values: ['1', PUBLIC.SP_HELLO('XX'), VARCHAR, '12', '2147483647', '0']
request3:
  exception: []
  keys: [resultSet, next]
  values: 'true'
request4:
  exception: []
  keys: [resultSet, getString, '1']
  values: 'HELLO: XX'
request5:
  exception: []
  keys: [resultSet, next, '#1']
  values: 'false'
request6:
  exception: []
  keys: DROP TABLE customers IF EXISTS
  values: 'false'
request7:
  exception: []
  keys: CREATE TABLE customers(id SERIAL, first_name VARCHAR(255), last_name VARCHAR(255))
  values: 'false'
request8:
  exception: []
  keys: DatabaseMetaData:supportsBatchUpdates
  values: 'true'
request9:
  exception: []
  keys: ['INSERT INTO customers(first_name, last_name) VALUES (?,?)', '1', John, '2',
    Woo, '1', Jeff, '2', Dean, '1', Josh, '2', Bloch, '1', Josh, '2', Long]
  values: ['1', '1', '1', '1']
request10:
  exception: []
  keys: ['SELECT id, first_name, last_name FROM customers WHERE first_name = ?', '1',
    Josh]
  values: ['3', ID, INTEGER, '4', '10', '0', FIRST_NAME, VARCHAR, '12', '255', '0',
    LAST_NAME, VARCHAR, '12', '255', '0']
request11:
  exception: []
  keys: [resultSet, 'SELECT id, first_name, last_name FROM customers WHERE first_name
      = ?', next]
  values: 'true'
request12:
  exception: []
  keys: [resultSet, 'SELECT id, first_name, last_name FROM customers WHERE first_name
      = ?', getLong, id]
  values: '3'
request13:
  exception: []
  keys: [resultSet, 'SELECT id, first_name, last_name FROM customers WHERE first_name
      = ?', getString, first_name]
  values: Josh
request14:
  exception: []
  keys: [resultSet, 'SELECT id, first_name, last_name FROM customers WHERE first_name
      = ?', getString, last_name]
  values: Bloch
request15:
  exception: []
  keys: [resultSet, 'SELECT id, first_name, last_name FROM customers WHERE first_name
      = ?', next, '#1']
  values: 'true'
request16:
  exception: []
  keys: [resultSet, 'SELECT id, first_name, last_name FROM customers WHERE first_name
      = ?', getLong, id, '#1']
  values: '4'
request17:
  exception: []
  keys: [resultSet, 'SELECT id, first_name, last_name FROM customers WHERE first_name
      = ?', getString, first_name, '#1']
  values: Josh
request18:
  exception: []
  keys: [resultSet, 'SELECT id, first_name, last_name FROM customers WHERE first_name
      = ?', getString, last_name, '#1']
  values: Long
request19:
  exception: []
  keys: [resultSet, 'SELECT id, first_name, last_name FROM customers WHERE first_name
      = ?', next, '#2']
  values: 'false'
